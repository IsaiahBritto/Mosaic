/** Shared recurrence mapping stub — full RRULE parity deferred to backlog. */
export function googleRecurrenceToMosaic(_rrule: string[]): null {
  return null;
}

export function mosaicRecurrenceToGoogle(_rule: unknown): string[] | undefined {
  return undefined;
}
